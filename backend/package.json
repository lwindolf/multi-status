{
        "name": "multi-status-backend",
        "version": "0.1",
        "main": "update.js",
        "type": "module",
        "dependencies": {
                "feed": "^5.2.1",
                "jsdom": "^22.1.0",
                "node-fetch": "^3.3.2"
        },
        "config": {
                "outputdir": "/var/www/multi-status"
        },
        "scripts": {
                "start": "sh -c \"node update.js $npm_package_config_outputdir\"",
                "systemd-start": "systemctl --user start multi-status-backend.service",
                "systemd-stop": "systemctl --user stop multi-status-backend.service",
                "systemd-install": "loginctl enable-linger $USER && mkdir -p ~/.config/systemd/user/ && cp multi-status-backend.service multi-status-backend.timer ~/.config/systemd/user/ && sed -i \"s|REPLACEME_WORKDIR|$(pwd)|\" ~/.config/systemd/user/multi-status-backend.service && systemctl --user daemon-reload && systemctl --user enable --now multi-status-backend.timer",
                "systemd-uninstall": "systemctl --user stop multi-status-backend.service && systemctl --user disable multi-status-backend.service && systemctl --user stop multi-status-backend.timer && systemctl --user disable multi-status-backend.timer && rm ~/.config/systemd/user/multi-status-backend.* && systemctl --user daemon-reload",
                "systemd-log": "journalctl --user -u multi-status-backend.service -f"
        }
}
